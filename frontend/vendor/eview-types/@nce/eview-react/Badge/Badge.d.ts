import React from 'react';
import { BadgeProps } from './types';
export default function Badge({ id, content, dot, max, offset, showZero, status, text, children, className, style, badgeClassName, badgeStyle, }: BadgeProps): React.JSX.Element;
