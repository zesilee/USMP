# USMP 前端页面设计规格说明书

**日期**: 2026-05-03
**版本**: v1.0
**设计范围**: 完整平台前端页面设计

## 一、整体设计原则

### 1.1 设计风格
- **企业级专业风格**: 采用 Element Plus 蓝灰色系，强调数据可读性和操作便捷性
- **操作优先**: 功能优先，减少不必要的视觉装饰
- **一致性**: 所有模块采用统一的交互模式和视觉语言

### 1.2 适配策略
- 仅桌面端优先（1024px+），移动端后续迭代
- 不做响应式移动端适配

## 二、整体布局设计

### 2.1 页面结构

```
┌─────────────────────────────────────────────────────────────────┐
│  HEADER: Logo | 全局搜索 | 设备状态指示器 | 通知 | 用户 | 主题切换 │
├──────────┬──────────────────────────────────────────┬───────────┤
│          │                                          │           │
│  SIDEBAR │              MAIN CONTENT                │  DETAIL   │
│  (固定)  │              (可滚动)                    │  (抽屉)   │
│          │                                          │           │
└──────────┴──────────────────────────────────────────┴───────────┘
```

### 2.2 尺寸规范
- **左侧导航栏**: 240px 固定宽度，可折叠（折叠后 64px），支持垂直滚动
- **顶部 Header**: 64px 高度，深色/浅色双主题支持
- **主内容区**: 自适应宽度，padding 24px，卡片间距 16px
- **右侧详情抽屉**: 560px 宽度，支持半屏和全屏切换

### 2.3 Sidebar 导航结构

```
📊 概览               → Dashboard
🖥️ 设备管理           → 设备列表/连接管理

📂 业务网络配置       ──┐
  ├─ 🔌 接口配置     → BusinessInterface
  ├─ 🏷️ VLAN配置    → BusinessVlan
  └─ 🛣️ 路由配置     → BusinessRoute

⚙️ 原生配置           → 点击后加载动态子菜单
  ├─ [加载中...]      → 调用 CRD 接口 /api/crd/models
  ├─ huawei-if       ← 动态生成
  ├─ huawei-vlan     ← 动态生成
  └─ ...

📋 操作日志           → 审计日志
⚙️ 系统设置           → 平台配置
```

### 2.4 导航交互细节
- 分组菜单点击展开/折叠，默认展开状态
- 当前激活菜单项有左侧蓝色边框指示
- 原生配置菜单项初次点击时调用后端接口获取子菜单
- 折叠模式下只显示图标，分组名称 tooltip 显示

## 三、仪表盘页面设计

### 3.1 页面布局

```
┌───────────────────────────────────────────────────────────┐
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐    │
│  │ 设备总数 │ │ 在线设备 │ │ 配置同步 │ │ 今日操作 │    │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘    │
│                                                           │
│  ┌───────────────────────────────────────────────────┐   │
│  │ 设备状态分布图（环形图）                           │   │
│  └───────────────────────────────────────────────────┘   │
│                                                           │
│  ┌───────────────────────────────────────────────────┐   │
│  │ 最近操作记录                                        │   │
│  └───────────────────────────────────────────────────┘   │
└───────────────────────────────────────────────────────────┘
```

### 3.2 设计要点
- **顶部统计卡片**: 4个核心指标，带趋势箭头（较昨日）
- **设备状态图表**: ECharts 环形图，展示在线/离线/异常分布
- **最近操作表格**: 展示最近20条配置操作记录，支持跳转到对应设备
- **刷新机制**: 数据每30秒自动刷新，支持手动刷新
- **异常告警**: 设备离线或配置失败时，对应卡片显示红色告警状态

## 四、设备管理页面设计

### 4.1 页面布局

```
┌───────────────────────────────────────────────────────────┐
│  [搜索设备IP/名称  [状态筛选▼]  [厂商筛选▼]  [刷新]  │
├───────────────────────────────────────────────────────────┤
│  ┌───────────────────────────────────────────────────┐   │
│  │ ✅  设备IP      │ 厂商  │ 型号  │ 状态  │ 操作 │   │
│  ├───────────────────────────────────────────────────┤   │
│  │ 设备列表表格                                    │   │
│  └───────────────────────────────────────────────────┘   │
└───────────────────────────────────────────────────────────┘
```

### 4.2 表格列定义
- 复选框（预留，不做批量操作）
- 设备IP + 设备名称
- 厂商 / 型号 / 固件版本
- 连接状态（在线✅ / 离线❌ / 异常⚠️）
- 最后同步时间
- 操作列：查看配置、连接测试、编辑设备

### 4.3 交互细节
- 点击行任意位置打开右侧设备详情抽屉
- 操作列按钮独立响应

## 五、配置管理页面设计（动态渲染架构）

### 5.1 前后端协作架构（CRD 声明式 API）

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              前端                                      │
│  ┌──────────────┐    ┌──────────────┐    ┌─────────────────────────┐  │
│  │              │    │              │    │                         │  │
│  │  创建/更新  │───→│  List&Watch  │───→│ 动态表单/表格渲染       │  │
│  │  Config CR  │    │  CR 状态变化 │    │                         │  │
│  │              │    │              │    │                         │  │
│  └──────────────┘    └──────────────┘    └─────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ HTTP
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                              后端                                      │
│  ┌──────────────┐    ┌──────────────┐    ┌─────────────────────────┐  │
│  │              │    │              │    │                         │  │
│  │  Config CR   │───→│  Controller  │───→│ 交换机 NETCONF 协议层   │  │
│  │  API Server  │    │  Reconciler  │    │                         │  │
│  │              │    │              │    │                         │  │
│  └──────────────┘    └──────────────┘    └─────────────────────────┘  │
│            ↑                                                             │
│            │                        ┌─────────────────────────┐        │
│            └────────────────────────│ 更新 CR Status          │        │
│                                     │ - 配置数据              │        │
│                                     │ - 同步状态              │        │
│                                     │ - 错误信息              │        │
│                                     └─────────────────────────┘        │
└─────────────────────────────────────────────────────────────────────────┘
```

### 5.2 架构原则

**声明式配置管理：**
1. 前端仅通过 CRD 接口创建/更新/删除 Config 自定义资源（CR）
2. 后端 Controller 通过 List & Watch 监控 CR 变化
3. Reconciler 循环处理 CR，执行实际的配置查询/下发
4. 后端将处理结果写入 CR 的 Status 字段
5. 前端通过 Watch 机制实时获取 CR 状态更新并渲染页面

**异步处理流程：**
1. 前端创建 CR Spec（期望状态）→ Pending 状态
2. 后端 Reconciler 发现 CR → 处理中 → Updating 状态
3. 后端执行 NETCONF 配置下发/查询 → 完成/失败
4. 后端更新 CR Status（Ready / Failed）
5. 前端 Watch 到状态变化 → 刷新 UI 显示最新状态

### 5.2 Config CR 定义

**Custom Resource 结构：**

```yaml
apiVersion: network.usmp.io/v1alpha1
kind: DeviceConfig
metadata:
  name: device1-interfaces
  labels:
    device: "192.168.1.1"
    module: "openconfig-interfaces"
spec:
  # Schema 定义 - 前端动态渲染表单
  schema:
    module: "openconfig-interfaces"
    title: "接口配置"
    fields:
      - path: "/interfaces/interface/name"
        type: "string"
        label: "接口名称"
        placeholder: "例如: GigabitEthernet0/0/1"
        required: true
        pattern: "^[a-zA-Z0-9/]+$"

  # 期望配置数据 - 用户在前端填写提交
  desired:
    interfaces:
      interface:
        - name: "GigabitEthernet0/0/1"
          enabled: true
          vlan: 100

status:
  # 处理状态: Pending | Updating | Ready | Failed
  phase: "Ready"

  # 实际配置数据 - 从交换机读取的真实配置
  actual:
    interfaces:
      interface:
        - name: "GigabitEthernet0/0/1"
          enabled: true
          vlan: 100

  # 最后同步时间
  lastSyncTime: "2026-05-03T14:30:00Z"

  # 错误信息（失败时）
  error: ""
```

### 5.3 CRD API 接口设计

**1. 创建 Config CR**
```
POST /api/crd/configs
Body: { metadata, spec }
```

**2. 更新 Config CR**
```
PUT /api/crd/configs/{name}
Body: { metadata, spec }
```

**3. 删除 Config CR**
```
DELETE /api/crd/configs/{name}
```

**4. List & Watch Config CR（SSE 流式）**
```
GET /api/crd/configs/watch?device={deviceIp}&module={module}

# SSE 事件流格式
event: ADDED | MODIFIED | DELETED
data: {完整 CR JSON}
```

**5. 获取 Schema 元数据**
```
GET /api/crd/schema/{module}
返回：字段定义列表（用于前端动态渲染）
```

### 5.4 通用控件映射表

| YANG 类型 | 前端控件 | 说明 |
|-----------|----------|------|
| boolean | ElSwitch | 开关组件 |
| enumeration | ElSelect | 下拉选择 |
| string | ElInput | 文本输入 |
| int/uint | ElInputNumber | 数字输入 |
| leafref | ElSelect/Cascader | 关联引用 |
| list | ElTable | 动态表格 |
| container | ElCollapse | 分组面板 |

### 5.5 页面布局

```
┌───────────────────────────────────────────────────────────┐
│  选择设备: [设备下拉选择▼ 192.168.1.1        ]          │
│  状态: [ 🔵 同步中 ] - 通过 Watch 实时更新状态徽章     │
├───────────────────────────────────────────────────────────┤
│  ┌───────────────────────────────────────────────────┐   │
│  │  (基于 CR status.actual 动态渲染的配置表格)       │   │
│  └───────────────────────────────────────────────────┘   │
│  [+ 新增配置项]                                           │
└───────────────────────────────────────────────────────────┘
```

### 5.6 配置提交流程（声明式异步）

1. **用户点击提交**
   - 前端校验表单
   - 显示配置差异预览（desired vs actual）

2. **用户确认提交**
   - 前端创建/更新 Config CR（仅更新 spec.desired）
   - 页面显示 "Pending" 状态徽章

3. **后端 Reconciler 处理**
   - Controller Watch 到 CR 变化
   - 执行 NETCONF 配置下发到交换机
   - 更新 CR status.phase = "Updating"

4. **前端 Watch 状态更新**
   - SSE 推送 MODIFIED 事件
   - 页面状态徽章更新为 "Updating"（蓝色旋转）

5. **处理完成**
   - 后端更新 CR status = Ready / Failed
   - 前端 Watch 到变化，更新表格和状态徽章
   - 显示成功/失败 Toast 提示

### 5.7 配置查询流程

1. **进入配置页面**
   - 前端发起 SSE Watch 连接
   - 按设备 + 模块过滤

2. **CR 不存在时自动创建**
   - 首次查询无 CR → 前端创建空 CR
   - 后端 Reconciler 自动从交换机拉取配置
   - 填充 status.actual 字段

3. **实时更新**
   - 任何配置变化通过 Watch 推送到前端
   - 页面自动刷新显示最新配置

## 六、原生配置页面设计（CRD 动态驱动）

### 6.1 动态菜单加载流程

1. 初次点击 "原生配置" 菜单
2. 调用后端接口 `GET /api/crd/models?type=native` 获取可用的 YANG 模型列表
3. 动态展开子菜单，显示所有模型名称
4. 缓存菜单数据，避免重复请求

### 6.2 后端接口设计

**GET /api/crd/models?type=native**
```json
{
  "models": [
    {
      "name": "huawei-if",
      "title": "华为接口配置",
      "vendor": "huawei",
      "description": "华为设备原生接口配置模型"
    }
  ]
}
```

### 6.3 组件复用
- 原生配置与业务配置 100% 复用同一套通用配置组件
- 仅通过 `module` 参数区分不同的 YANG 模块
- 相同的 CRD API、相同的 Watch 机制、相同的渲染逻辑

### 6.4 CR 命名规范

业务配置 CR：`{deviceIp}-{module}`
- 例：`192.168.1.1-openconfig-interfaces`

原生配置 CR：`{deviceIp}-native-{vendor}-{module}`
- 例：`192.168.1.1-native-huawei-if`

## 七、操作日志和系统设置

### 7.1 操作日志页面
- 支持按时间范围、设备、操作类型、状态筛选
- 点击行查看操作详情（修改前后 diff、错误信息）
- 支持导出 CSV

### 7.2 系统设置页面
- 全局设置：缓存 TTL、同步间隔、超时时间、重试次数
- 主题设置：浅色模式 / 深色模式 / 跟随系统

## 八、技术实现要点

### 8.1 技术栈
- **框架**: Vue 3 + TypeScript + Vite
- **UI 组件库**: Element Plus
- **代码编辑器**: Monaco Editor（原生配置高级模式）
- **图表**: ECharts
- **HTTP 客户端**: Axios
- **状态管理**: Pinia（仅管理全局 UI 状态）

### 8.2 组件架构

```
src/
├── components/
│   ├── layout/
│   │   ├── MainLayout.vue      # 主布局容器
│   │   ├── Sidebar.vue         # 侧边导航（静态+动态混合）
│   │   ├── Header.vue          # 顶部栏
│   │   └── DetailDrawer.vue    # 右侧详情抽屉
│   ├── config/
│   │   ├── ConfigPage.vue      # 通用配置页面容器
│   │   ├── DynamicForm.vue     # 动态表单渲染器
│   │   ├── DynamicTable.vue    # 动态表格渲染器
│   │   └── FieldRenderer.vue   # 字段→控件映射器
│   └── common/
│       └── StatusBadge.vue     # CR 状态徽章（Pending/Updating/Ready/Failed）
├── composables/
│   └── useDeviceConfig.ts      # CRD Watch 封装 Composable
├── stores/
│   ├── menu.ts                 # 动态菜单状态
│   └── device.ts               # 当前设备状态
├── api/
│   ├── crd.ts                  # CRD REST + SSE Watch 接口
│   └── device.ts               # 设备接口
└── utils/
    └── yang-schema.ts          # YANG Schema 解析工具
```

### 8.3 useDeviceConfig Composable 设计

```typescript
const {
  configCR,        // 当前 CR 响应式对象
  isLoading,       // 加载状态
  isSyncing,       // 同步中状态
  error,           // 错误信息
  save,            // 创建/更新 CR
  remove,          // 删除 CR
  refresh          // 手动刷新
} = useDeviceConfig(deviceIp, moduleName)
```

**核心功能：**
- 自动建立 SSE Watch 连接
- 响应式 CR 状态管理
- 自动重连机制
- 统一的 save/remove API

### 8.4 核心实现原则
1. **纯 CRD 接口**: 前端仅通过 CRD API 与后端交互，无其他私有接口
2. **声明式优先**: 仅更新 Spec，不直接调用 RPC 接口
3. **Watch 驱动**: 所有状态变化通过 SSE Watch 实时推送
4. **无状态设计**: 前端不缓存业务数据，所有数据来自 CR Status
5. **组件复用最大化**: 业务配置和原生配置 100% 复用同一套组件
6. **动态渲染优先**: 所有表单基于 Schema 动态生成
7. **类型安全**: TypeScript 严格模式，CR 结构完整类型定义
