apiVersion: v1
kind: Service
metadata:
  name: usmp-frontend
  namespace: usmp-system
spec:
  type: NodePort
  selector:
    app: usmp-frontend
  ports:
  - name: http
    port: 80
    targetPort: 80
    nodePort: 30081
    protocol: TCP
