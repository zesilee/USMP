# ygot 生成配置（make gen-yang 消费；yang_path 相对仓库根）
# 示例北向派生模型（crd-to-yang-codegen）：crd2yang 从示例 BusinessVlanNet CRD
# 反向生成的 usmp-business-vlan-net。独立包承载（不并入 business）：demo 生命周期
# 独立，北向正式 CRD 到位接线时可整目录替换/退役（design D7）。
yang_path=backend/internal/yang/models
modules=usmp-business-vlan-net
generate_fakeroot=true
compress_paths=false
